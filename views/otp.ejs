<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>OTP Verification</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .otp-box {
            max-width: 400px;
            margin: 0 auto;
            margin-top: 50px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 8px;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="otp-box">
            <h2 class="text-center mb-4">OTP Verification</h2>
            <form action="/otpverification" method="post">
                <div class="mb-3">
                    <label for="otpInput" class="form-label">Enter OTP</label>
                    <input type="text" name="otp" class="form-control" id="otpInput" placeholder="Enter OTP">
                </div>
                <button type="submit" class="btn btn-primary">Verify</button>
                
                <p id="expirationTime" class="mt-3">OTP Expires in <span id="countdown">60</span> seconds</p>
            </form>
            <form action="/resend_otp" method="post">
                <button type="submit" class="btn btn-secondary ms-2" id="resendButton">Resend OTP</button>
            </form>
        </div>
    </div>

    <!-- <script>
        let countdown = 60; // Initial countdown time in seconds
        const countdownDisplay = document.getElementById('countdown');
        const resendButton = document.getElementById('resendButton');

        // Function to update countdown timer
        function updateCountdown() {
            if (countdown > 0) {
                countdown--;
                countdownDisplay.textContent = countdown;
                setTimeout(updateCountdown, 1000); // Update every second
            } else {
                // If countdown reaches zero, enable resend button or perform necessary action
                resendButton.disabled = false;
                countdownDisplay.textContent = 'Expired';
            }
        }

        // Start the countdown initially
        updateCountdown();

        // Simulating Resend OTP functionality
        resendButton.addEventListener('click', function () {
            // Reset countdown and disable the button
            countdown = 60;
            countdownDisplay.textContent = countdown;
            resendButton.disabled = true;

            // Start countdown again
            updateCountdown();
        });
    </script> -->
</body>

</html>
